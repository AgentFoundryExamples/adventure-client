# Development Environment Configuration
#
# This file contains safe default values for local development.
# These are MOCK values and should not be used for real Firebase authentication.
#
# TO USE REAL FIREBASE IN DEVELOPMENT:
#   1. Copy this file to .env.local:
#      cp .env.development .env.local
#   
#   2. Update .env.local with your actual Firebase configuration from Firebase Console
#      See docs/firebase-setup.md for detailed instructions
#
#   3. .env.local is in .gitignore and will not be committed
#
# IMPORTANT: 
#   - .env.local takes precedence over .env.development
#   - Restart dev server (npm run dev) after changing environment files
#   - Never commit real Firebase credentials to version control

# ============================================================================
# API Base URLs (Development - Local Backend Services)
# ============================================================================
# Points to locally running backend services on standard ports
# Update these if your local backend uses different ports

VITE_DUNGEON_MASTER_API_BASE_URL=http://localhost:8001
VITE_JOURNEY_LOG_API_BASE_URL=http://localhost:8002

# ============================================================================
# Firebase Configuration (Development - Mock/Test Values)
# ============================================================================
# These are MOCK values that allow the app to build but won't work for real authentication.
# Replace these in .env.local with your actual Firebase development project configuration.
#
# To get real values:
#   1. Create a Firebase project at https://console.firebase.google.com/
#   2. Go to Project Settings > General > Your apps
#   3. Add or select a Web app
#   4. Copy the config values to .env.local
#
# See docs/firebase-setup.md for step-by-step instructions.

VITE_FIREBASE_API_KEY=mock-api-key-dev
VITE_FIREBASE_AUTH_DOMAIN=mock-project-dev.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=mock-project-dev
VITE_FIREBASE_STORAGE_BUCKET=mock-project-dev.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=000000000000
VITE_FIREBASE_APP_ID=1:000000000000:web:mockappiddev
VITE_FIREBASE_MEASUREMENT_ID=G-MOCKIDDEV
